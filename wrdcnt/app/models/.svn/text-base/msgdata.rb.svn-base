class Msgdata < ActiveRecord::Base

        #def self.getCounts(session_id, srch_str)
                
         #   a =  Hash.new
          #  a['sad'] = where("score < 0 and session_id = ? and search_string = ?",session_id,srch_str).count
           # a['neutral'] = where("score = 0 and session_id = ? and search_string = ?",session_id,srch_str).count
            #a['happy'] = where("score > 0 and session_id = ? and search_string = ?",session_id,srch_str).count
           
          #  return a
        #end
        
end
